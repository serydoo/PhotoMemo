# MemoMark 2.1.2 (79) 同步清单

## 基线

- 日期：2026-08-12（Asia/Shanghai）
- 用户指定范围起点：2026-08-09（周日）约 14:12。
- Git 相邻基线：`588792d`（2026-08-09 14:06:59，`2.1.0 (76)` 发布检查点）。
- 已同步但未上架中间候选：`2.1.1 (77)`。
- 目标版本：`2.1.2 (79)`
- 状态：`Source Checkpoint Ready; Store Delivery Evidence Open`

## 正式同步范围

- 2.1.1 未发布候选中的记忆对象切换一致性修复；
- 对象头像构图、裁切、缩放和圆形预览改进；
- 记忆对象异步持久化提交、失败回滚和保存后上下文投影；
- 对象、时间锚点、记忆配置、输出配置、Logo 与四区域草稿的一致性测试；
- 自选 Logo 圆形资源、预览与 Renderer 一致性；
- 自选 Logo 绝对运行时路径与可携带相对路径的双向转换；
- 首页“怎么记录”常驻说明与底部按钮文案修正；
- Job 级真实历史封面、稳定多图代表图、数量角标、旧数据兼容和容量治理；
- 通知附件释放、缺失封面引用自愈和两阶段孤儿文件清理；
- 周日基线之后已经同步的产品支持文档与发布同步规范；
- 双语资源、版本字段、README、CHANGELOG、CURRENT_STATUS 与本版本材料。

## 排除范围

- 私人照片、人物与地点数据、真机容器副本、日志、截图和录屏；
- 签名构建、DerivedData、归档包和个人 Xcode 状态；
- 未冻结 V4 研究与未经验证的生产声明；
- 私人测试图片 `IMG_*.png` 和下载目录中的验收截图；
- TestFlight/App Store 上传产物，除非在本轮发布验证后获得独立外部交付授权。

## 当前证据与停止点

- Logo 保存、重新进入、卡片预览和最终呈现已由真机人工验收确认。
- 单图与多图真实历史封面、稳定代表图和数量角标已由真机截图确认。
- 历史封面聚焦模型、持久化、Presenter、缓存治理和旧数据兼容测试通过。
- 完整 macOS `PhotoMemoTests` 通过：`1362` tests、`203` suites、`0` failures。
- 通用 iOS 构建、签名 `iphoneos` 构建和 Release archive 均通过；归档内 App、Share Extension、Widget Extension 均为 `2.1.2 (79)`。
- 归档 App 通过 `codesign --verify --deep --strict`，嵌套扩展团队标识一致；归档包和签名产物仅保留在本地，不进入 Git。
- 签名真机构建已在 iPhone 17 Pro Max 原位安装并启动，未卸载 App、未清除本地数据；Logo 保存/重进、圆形预览与输出、多图稳定代表图和数量角标已完成手动验收。
- 本地发行导出不属于本轮交付路径；当前 Mac 仅有 `Apple Development: serydoo@163.com`，不作为商店候选的发行签名环境，也不宣称本地生成 IPA。
- 本轮按用户指定由 Xcode Cloud 负责发行构建与签名；Xcode Cloud 构建/上传和 App Store 提交尚未在本工作区执行，不能由源码同步、归档或真机安装成功推断完成。
- `TX-001`、`BP-001` 和历史条件性认证边界没有因本轮验收关闭。

## 授权

- 用户已明确授权整理、Git 暂存、提交和推送 GitHub。
- 用户已表达继续商店交付的意图；TestFlight 上传或 App Store 提交仍按同步规范在实际外部动作点单独确认并记录。

## 源码同步结果

- 已推送分支：`main`
- 发布同步提交：`382fc1a`（源码与正式同步范围）
- 交付路径与真机证据记录：`e4a50ce`
- 当前远端 `origin/main` 与本地 `HEAD` 已一致，工作区干净。
