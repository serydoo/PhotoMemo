# MemoMark 2.1.2 (86) 同步清单

## 基线与目标

- 上一次已推送检查点：`2dc0f21d43f185f4c666591c8ee56c1254065256`（短哈希 `2dc0f21`，2026-08-14；当前 `HEAD` 与 `origin/main` 一致）。
- 本轮范围终点：当前工作树中从该检查点开始的用户交付可靠性整改、测试、双语资源和版本资料。
- marketing version：`2.1.2`。
- build number：`86`，从已推送的 `85` 顺延一位。
- 目标分支：当前 `main`。
- 本轮授权：准备整理、更新版本字段和发布材料；不包含 Git 提交、推送、TestFlight 上传或 App Store 提交授权。
- 状态：`Source Checkpoint Ready; Release Evidence Open`。

## 正式同步范围

- `ResolvedContentValidator` 和最终 resolved content 的空结果拦截；
- `BatchDeliverySummary`、原始请求数统计和 `skippedRequiringAttentionCount` 语义；
- Share Extension handoff 未开始时的明确状态和可恢复交接路径；
- `memomark://processing/<jobID>` 通知路由、通知点击事件和对应 Processing Status/Job 聚焦；
- `processing.contentValidation.failed` 诊断以及既有脱敏支持编号边界；
- 中英文 `Localizable.strings` 新增交付状态和内容校验文案；
- 对应的批量通知、深链、背景处理、生产配置、诊断、Share Extension 和状态架构契约测试；
- `2.1.2 (86)` 版本字段、应用内更新日志、CHANGELOG、README、CURRENT_STATUS、HANDOFF 和本版本四份材料。

## 本地保留范围

- 真机逐项验收产生的 xcresult、截图、录屏、日志、媒体、设备容器和个人 Xcode 状态；
- 私人照片、人物/地点数据、签名产物、归档包、DerivedData、临时构建输出和本地诊断导出；
- 用户暂时保留的 20 张混合验收矩阵及其未来手动测试结果；
- 未冻结的 V4 研究、实验资产和无法证明属于本轮交付可靠性范围的材料。

## 待决定范围

- 实体 iPhone Share -> Processing -> Notification -> Apple Photos 20 张混合交付；
- 通知冷启动、后台、终止态点击和视觉/无障碍验收；
- Live Photo、去 EXIF、社交软件保存图和无有效内容配置的逐项用户验收；
- StoreKit、TestFlight、App Store Connect、商店截图和生产认证；
- Git 暂存、提交、推送及其他外部交付动作。

## 版本事实源分发

| 受众 | 文件 | 边界 |
| --- | --- | --- |
| 应用内用户 | `Source/PhotoMemo/PhotoMemo/zh-Hans.lproj/Localizable.strings`、`en.lproj/Localizable.strings` | 温和描述交付结果、批量统计、失败定位和本地处理 |
| App Store | `2026-08-18-2.1.2-app-store-whats-new.md` | 最短用户价值，不含测试术语或内部状态 |
| TestFlight | `2026-08-18-2.1.2-testflight-notes.md` | 20 张混合矩阵、通知点击、反馈字段和隐私提醒 |
| 内部同步 | 本文件、`release-notes`、`CHANGELOG.md`、`README.md`、`Docs/CURRENT_STATUS.md` | 基线、范围、证据、风险、排除项和授权边界 |

## 同步前检查表

- [x] 已确定范围起点 `2dc0f21`、marketing version `2.1.2` 和 build number 顺延目标 `86`。
- [x] 已将当前工作树按正式同步、本地保留和待决定三类整理；当前未发现新的未跟踪文件。
- [x] App、Share Extension、Widget Extension、QA target 和 test target 的工程版本字段统一为 `2.1.2 (86)`；generic iOS 产物中的主 App、Share Extension 和 Widget Extension 已逐一核验。
- [x] 版本总表、应用内、App Store、TestFlight、CHANGELOG、README、CURRENT_STATUS 和 HANDOFF 已完成一致性核对。
- [x] 双语资源格式和键集合检查通过；应用内更新日志契约测试通过。
- [x] `git diff --check` 通过。
- [x] 聚焦测试、完整测试和对应构建分别记录；Xcode Beta 的既有 warning 与 macOS 独立 build 工具异常均单独记录，没有写成完整通过证据。
- [x] 自动化、真机安装启动、手动关键路径、StoreKit、App Store Connect 和生产认证分开记录；真机和外部交付仍未关闭。
- [x] 已核对未跟踪文件；私人照片、设备材料、归档包、签名产物和本地诊断不进入同步范围。
- [ ] Git 暂存、提交和推送：未授权、未执行。
- [x] TestFlight 上传和 App Store 提交：未授权、未执行。

## 当前证据与停止点

- 当前源码整改和版本顺延后的完整 `PhotoMemoTests` 为 `1455 passed / 0 failed / 1 skipped`，结果包为 `/tmp/MemoMarkDeliveryBuild86Full/Logs/Test/Test-PhotoMemoTests-2026.08.18_11-05-27-+0800.xcresult`。
- generic iOS unsigned build 产物为 `/tmp/MemoMarkDeliveryBuild86iOS/Build/Products/Debug-iphoneos/PhotoMemoiOS.app`，主 App、Share Extension、Widget Extension 均为 `2.1.2 (86)`；macOS 独立 build 保留 Xcode Beta 工具异常现场。
- 本轮所有本地验证产物仍只存于临时目录，不进入 Git 同步范围。
- 构建成功、自动化测试通过和源码同步准备完成，都不等于实体设备、Apple Photos、StoreKit、TestFlight、App Store 或生产认证已完成。
- 完成本清单的本地核验后，最高只能达到 `Source Checkpoint Ready; Release Evidence Open`；继续暂存、提交、推送必须由用户另行明确授权。
