# MemoMark 2.2.2 (90) 同步清单

## 1. 版本与基线

- Marketing version：`2.2.2`
- Build number：`90`
- 最近一次 GitHub 推送：`c0f11a7aa36b7a3b0107b3e6003214e57376e074`
- 推送时间：2026-08-24 06:49:40 +0800（`main == origin/main`）
- 同步范围：`c0f11a7..current working tree`
- 当前本地工程：`/Users/rui/Desktop/PhotoMemo/Source/MemoMark/MemoMark.xcodeproj`
- 当前状态：`Version Locked; Release Evidence Open`
- Git 基点确认：`main == origin/main == c0f11a7`；本轮没有需要拉取的远端提交。
- 当前工作区仍保留上一轮内部迁移形成的分阶段索引状态。数量会随本轮清理继续变化，
  因此不在清单中固化瞬时计数；同步前以 `git status` 的最终结果为准。本轮尚未获得
  Git 暂存、提交或推送授权。

本清单只整理当前工作区，不授权 Git 暂存、提交、推送、TestFlight 上传或 App
Store 提交。自动同步脚本也没有执行。

## 2. 工作区分类

### 正式同步候选范围

- `Source/MemoMark/` 当前源码、四语资源、工程文件、target、scheme 和 plist。
- `Tests/MemoMarkTests/`、`Tests/MemoMarkUITests/` 当前测试与模块引用。
- 当前设备 QA、运行时证据和自动同步脚本入口。
- `CHANGELOG.md`、`README.md`、`README_EN.md`、`Docs/CURRENT_STATUS.md`、
  `HANDOFF.md` 中本轮新增或更新的当前状态信息。
- 本目录下的 `2026-08-26-2.2.2-*` 五份版本材料，包括 App Review 材料。
- `Docs/Outreach/2026-08-26-MemoMark-2.2.2-XHS.md` 公开软文草稿。
- `Docs/02_Architecture/Renderer_Output_And_Style_Content_Independence_Spec.md` 渲染
  输出契约与样式内容隔离规范。
- `Docs/02_Architecture/MemoMark_Naming_Migration_Spec.md` 内部命名迁移与兼容值边界。
- `Docs/01_Product/V4_Inline_Module_Editor_Design_Research_2026-08-26.md` 当前录入框研究与
  方案 A 的产品/交互依据。
- `Docs/01_Product/V4_UI_System_Polish_2026-08-25.md` 当前 UI 升级记录与验证边界。
- `Tests/MemoMarkTests/ArchitectureTests/SubjectAvatarAssetOptimizationServiceTests.swift` 头像
  资源清理与路径边界回归；Xcode 文件系统同步组会自动将其纳入 `MemoMarkTests`，本轮聚焦
  构建已实际编译该文件。
- `Tests/MemoMarkTests/Support/MemoMarkTestPaths.swift` 与相关 source-contract tests：
  从当前 checkout 推导仓库根目录，替代开发者机器专属绝对路径。

### 本地保留范围

- 私人照片、私有数据集、设备截图、诊断导出、归档包、签名产物、DerivedData
  和个人 Xcode 状态。
- `output/` 中的历史图标和实验资产，除非本轮明确指定为发布资产。
- 既有历史 release note、历史审计和历史 handoff 内容；它们保留原始版本事实。
- 2026-08-22 的 2.1.4 与 2026-08-25 的 2.2.1 未跟踪草稿已在本轮清理删除；不进入
  2.2.2 候选，也不再作为本地待分类文件保留。

### 待决定范围

- 物理 iPhone 本轮重新安装、启动和 Apple Photos 人工全链路证据。
- Classic White Live Photo 底栏、Minimal 用户组合内容、样式切换独立性的人工验收。
- TestFlight 上传、StoreKit Production 交易、App Store Connect 元数据和正式提交。
- renderer 导出命名测试中仍存在的 3 个 fixture-dependent case。
- `TX-001`、`BP-001` 及 superseding production certification。

## 3. 不可变兼容值

以下值不得因为产品改名或版本更新而改变：

- `com.serydoo.PhotoMemo`
- `com.serydoo.PhotoMemo.iOS`
- `com.serydoo.PhotoMemo.iOS.ShareExtension`
- `com.serydoo.PhotoMemo.iOS.WidgetExtension`
- `group.com.serydoo.PhotoMemo`
- `photomemo.*` UserDefaults keys
- `com.serydoo.PhotoMemo.batch-processing`
- `com.serydoo.PhotoMemo.iOS.memomarkplus.lifetime`
- `photomemo://` 旧 URL scheme 的解析能力
- 已有 Share 诊断域和持久化 Codable 语义

内部 target、scheme、Swift module、文件名和开发脚本已使用 MemoMark 命名，
但不改变上述已发布身份和数据协议。

## 4. 版本材料映射

| 材料 | 文件 | 用途 |
| --- | --- | --- |
| 版本总表 | `2026-08-26-2.2.2-release-notes.md` | 用户价值、工程边界、验证和未关闭证据 |
| App Store | `2026-08-26-2.2.2-app-store-whats-new.md` | 简体中文和 U.S. English 此版本的新内容 |
| TestFlight | `2026-08-26-2.2.2-testflight-notes.md` | 可执行测试路径、反馈字段、隐私提醒 |
| 同步清单 | 本文件 | 基线、范围、排除项、兼容值、授权和停止点 |
| App Review | `2026-08-26-2.2.2-app-review-materials.md` | 推广文本、审核说明、元数据和提交前核对 |
| 公共媒体 | `../Outreach/2026-08-26-MemoMark-2.2.2-XHS.md` | 小红书软文、标题、标签和发布前注意事项 |

## 5. 版本字段核对

- `project.pbxproj` 中所有 `MARKETING_VERSION` 均为 `2.2.2`。
- `project.pbxproj` 中所有 `CURRENT_PROJECT_VERSION` 均为 `90`。
- 主 App、iOS、Share Extension、Widget、测试和 Device QA 的版本字段统一。
- 应用内 Settings → Version 从 Bundle 读取版本和构建号；本轮未硬编码版本文本。
- 四语 `settings.release_notes.*` 资源已同步为 2.2.2 面向用户的更新内容。

## 6. 已完成的本地验证

- `xcodebuild -list`：通过。
- macOS `MemoMark` Debug build：通过。
- 通用 iOS `MemoMarkiOS` Debug build：通过。
- `MemoMarkShareExtension` build：通过。
- `MemoMarkWidgetExtension` build：通过。
- `MemoMarkDeviceQA` build-for-testing：通过。
- 配置、队列、诊断、共享容器、持久化和深链接 focused tests：通过。
- Python 脚本测试：`117` 项通过。
- 4 个本地化资源、5 个 Info.plist 共 9 个资源 lint，Shell syntax、
  `git diff --cached --check`、`git diff --check`：通过。
- 本轮编辑器 Option A 候选已重新签名，核对为 `com.serydoo.PhotoMemo.iOS` / `2.2.2 (90)`；
  CoreDevice 已恢复可用，候选已覆盖安装并启动到配对 iPhone 17 Pro Max，视觉验收待产品负责人
  在设备上继续，不使用模拟器替代。
- 混排删除展示已由产品负责人在实体设备确认正常；自定义文字与模块之间的退格不会
  再从最右侧模块开始删除。最新候选已加入固定中心可视 caret，光标垂直稳定性仍需产品负责人
  在设备上完成空白、文字、模块和混排状态的人工验收；大字号和样式切换仍独立待验收。
- Renderer / 样式持久化 / 配置生命周期 focused tests：通过；新增样式内容契约、
  Minimal slot A 照片说明投影和样式独立性回归均通过；固定中心 caret 新候选已完成设备重装
  与启动，上述输出路径仍待产品负责人在设备上人工验收。
- Card Content Editor 紧凑布局、TextKit 光标度量、统一胶囊、失效降级、结构化复制/粘贴和
  undo 已纳入本地 UI 构建与实体设备人工验收清单；当前需重点检查空白/文字/模块光标垂直居中、
  模块胶囊高度、失效表达式可读性、Classic White 多区域标题列距离和大字号标题宽度。当前
  相关 focused suite 为 `53/53`。
- 仓库路径清理后的 10 个 architecture/source-contract suite 为 `59/59` 通过；所有相关测试
  从当前 checkout 解析源码文件，不再依赖 `/Users/rui/Desktop/MemoMark` 这类机器专属路径。
- 删除退役临时 iOS 双入口并清理 Xcode target 例外后，generic iOS `MemoMarkiOS` 与
  `MemoMarkShareExtension` Debug build 均通过。
- 本轮进一步修复 TextKit 混排退格定位：删除只作用于 live caret 左侧紧邻的文字/模块；
  编辑器明确为单行输入面，光标纵向固定在输入面中心，并以统一 28pt 行盒稳定空白、
  文字和模块状态；标题列按单区域 60pt / 多区域 36pt，间距为 4pt。方案 A 另外统一
  两套胶囊视觉度量，保留模块 key，补齐失效降级、结构化复制/粘贴和编辑撤销。
- 最新全量 `MemoMarkTests` 为 `1,551` passed、`30` failed、`1` skipped，共 `1,582` 项；
  较整理前记录的 `47` 个失败减少 `17` 个。方案 A 前的 iPhone UI/source-contract baseline
  为 `47/47`，当前相关 suite 为 `53/53` 通过。此前报告的 `10` 个 UI 基线失败来自过期的
  source-contract 断言，已与当前正式 UI 结构对齐，并非产品内部更名造成。剩余 30 个失败分布在
  既有 UI/source-contract、`3` 个 fixture-dependent export naming case 与 `2` 个 export
  responsibility contract；Renderer / style / mutation focused run 的本轮变更行为均通过，
  不把剩余基线解释为本轮仓库整理回归，也不据此宣称完整生产认证。

## 7. 版本风险

1. 构建号 `90` 已按产品负责人要求保留，但上传前必须确认 App Store Connect
   没有占用同一应用的 `90`。若服务端已使用，应先取得新的构建号决定。
2. 全量测试不是全绿；发布前不能把本候选表述为完整生产认证。
3. 本轮 Option A 候选已完成物理设备安装和启动；Apple Photos 人工回归、静态照片、Live Photo
  及保存回读仍待产品负责人检查，因此不能把本地构建通过表述为审核或发布通过。
4. App Store、TestFlight、StoreKit Production 和 Apple 审核状态均未改变。

## 8. 授权与停止点

- [x] 已按最近一次 GitHub 推送确定范围起点。
- [x] 已锁定 `2.2.2 (90)` 版本材料和工程字段。
- [x] 已分类正式同步、本地保留和待决定内容。
- [x] 已完成当前本地构建、聚焦测试和静态检查。
- [ ] 未获得本轮 Git 暂存、提交和推送授权。
- [ ] 未获得 TestFlight 上传授权。
- [ ] 未获得 App Store 提交授权。
- [x] 本轮 Option A 候选已完成真机安装和启动；完整人工回归仍由产品负责人执行。
