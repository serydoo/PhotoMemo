# MemoMark 2.1.0 (76) 同步清单

## 基线

- 用户指定范围起点：2026-08-06 周四 16:08（Asia/Shanghai）
- 相邻 Git 基线：`da2a77ad077ee02c9909f69ef6523f3c8ece08dc`，提交时间 16:02:56
- 当前分支：`main`
- 目标版本：`2.1.0 (76)`
- 当前状态：`Version Locked; Release Evidence Open`

## 正式同步范围

### 产品实现

- 四区 TextKit 卡片内容编辑、区域导航、预览连续性与键盘/完成边界；
- 配置中心、记忆对象、时间锚点、设置、欢迎与任务页面的有界体验整理；
- App 内备用选图、任务重试、Share 错误和 Commerce 重复操作反馈；
- “今天时间答案”界面投影与 Apple Photos 动作文案精度修正。

### 测试与资源

- 对应 Swift 契约与行为测试；
- 简体中文和英文 Localizable.strings；
- `2.1.0 (76)` 的所有 Xcode product/test 版本字段。

### GitHub 首页与项目文档

- `README.md` 作为 GitHub 仓库首页；
- `README_EN.md` 作为英文产品入口；
- Design System、产品语言指南、V4 有界 UI 记录、CURRENT_STATUS 与 CHANGELOG；
- 应用内、App Store、TestFlight 发布材料与长期同步规范。

## 本地保留、不进入同步

- 私人照片、人物/地点数据与私有研究数据集；
- 真机截图、屏幕录制、设备日志与本地诊断导出；
- DerivedData、Archive、IPA、签名包、描述文件和个人 Xcode 状态；
- 尚未进入本工作区 diff 的未冻结外部研究和候选素材。

当前 Git 状态未发现上述本地保留产物进入待同步文件列表。提交前仍须按实际 `git status` 再检查一次。

## 已完成验证

- 全部 8 组 marketing version 为 `2.1.0`；全部 10 组 build number 为 `76`；
- 简体中文与英文 Localizable.strings：`plutil -lint` 通过；
- `git diff --check` 通过；
- `V1ReleaseNotesContractTests`：4/4 通过；
- 受发布整理影响的 6 个契约测试套件：114/114 通过；
- 完整 `PhotoMemoTests`：1,349 项通过、1 项既有跳过、0 失败；
- `PhotoMemoiOS` unsigned Debug build：通过；
- `PhotoMemoShareExtension` unsigned Debug build：通过；
- `PhotoMemoWidgetExtension` unsigned Debug build：通过。

完整测试仍报告既有的测试宿主 UTType 声明与 fixture QoS 运行时警告；本轮没有把这些警告解释为发布认证通过，也没有借本次同步扩大修复范围。

## 同步前仍需完成

- 生成并核对签名候选包中的 App/Share/Widget 实际 Bundle 版本；
- 在目标真机覆盖安装并分别记录安装、启动和关键路径手动验收；
- 核对 App Store Connect 的版本列车、IAP 关联、设备支持、截图和隐私元数据；
- 本次用户授权仅覆盖 Git 暂存、提交与推送；上传 TestFlight 或提交 App Store 仍需独立执行与核验。

## 建议提交结构

为保留清楚的验证故事，正式同步建议按以下顺序形成提交：

1. 卡片内容编辑与页面体验整理；
2. 失败恢复与 Commerce 操作反馈；
3. `2.1.0 (76)` 版本字段、双语发布材料、README 与同步规范。

若产品负责人希望发布候选使用单一提交，可在最终验证通过后合并为一个 release checkpoint；不得在验证前为了整理历史而改写已存在的远端提交。
