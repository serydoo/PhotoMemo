# MemoMark 2.2.2 (91) 同步清单

## 1. 版本与基线

- Marketing version：`2.2.2`
- Build number：`91`
- 最近一次 GitHub 推送：`916fc843680181dbfe1a619e05bfc9c7e38aa027`
- 推送时 `main == origin/main`；本轮同步范围：`916fc84..current working tree`
- 当前状态：GitHub `main` sync authorized; TestFlight/App Store evidence open
- 当前工程：`/Users/rui/Desktop/PhotoMemo/Source/MemoMark/MemoMark.xcodeproj`

## 2. 正式同步范围

- `Source/MemoMark/` 当前源码、四语资源、工程文件、target、scheme 和 plist。
- `Tests/MemoMarkTests/` 当前测试与 source-contract 覆盖。
- `AGENTS.md`、`CHANGELOG.md`、`README.md`、`README_EN.md`、`HANDOFF.md`、`Docs/CURRENT_STATUS.md`。
- `Docs/03_Engineering/` 本轮输入几何规范、编辑器 UI pass 和审查修复记录。
- 本目录的五份 `2026-08-27-2.2.2-*` 版本材料；旧 2026-08-26 构建 90 材料保留历史事实。

## 3. 本地保留范围

私人照片、设备截图、诊断导出、归档包、签名产物、DerivedData、`.xcresult`、`output/` 历史实验资产、Xcode 用户状态和其他未纳入工程的本地材料不进入提交。

## 4. 版本字段与兼容值

- `project.pbxproj` 中所有 `MARKETING_VERSION` 应为 `2.2.2`。
- `project.pbxproj` 中所有 `CURRENT_PROJECT_VERSION` 应为 `91`，覆盖 App、iOS、Share Extension、Widget、测试和 Device QA。
- `com.serydoo.PhotoMemo`、`com.serydoo.PhotoMemo.iOS`、App Group、`photomemo.*`、后台任务、StoreKit 商品 ID 和旧 `photomemo://` 解析能力保持不变。
- Settings 版本文案从 Bundle 读取，不硬编码构建号。

## 5. 验证记录

- [x] `xcodebuild -list`：通过，6 个方案均可解析。
- [x] macOS `MemoMark` Debug build：明确 arm64 目的地通过，产物为 2.2.2 (91)。首次无明确目的地命令触发 Xcode Beta 的 exit-0/no-output 工具异常，未作为通过证据。
- [x] 通用 iOS `MemoMarkiOS` Debug build：通过，产物为 2.2.2 (91)。
- [x] `MemoMarkShareExtension` build：通过，产物为 2.2.2 (91)。
- [x] `MemoMarkWidgetExtension` build：通过，产物为 2.2.2 (91)。
- [x] `MemoMarkDeviceQA` build-for-testing：通过。
- [x] 当前 `MemoMarkTests` 全量测试：`1603 passed / 0 failed / 1 skipped`，结果 `Passed`；发现 2 条既有 QoS runtime warning，未产生失败。
- [x] 几何、编辑器 source-contract 和发布契约均包含在全量通过结果内；此前独立几何 focused run 仍为 8/8，通过。
- [x] Python 脚本测试：`117` 项通过；Shell 脚本语法检查通过；4 个本地化资源与 Share Extension Info.plist lint 通过。
- [x] `git diff --check`、版本字段静态核对、未跟踪/私有产物审查：通过；12 个 `CURRENT_PROJECT_VERSION` 为 91，10 个 `MARKETING_VERSION` 为 2.2.2。
- [ ] 91 号实体 iPhone 17 Pro Max 人工视觉与 Apple Photos 全链路：91 号签名包已在配对设备上覆盖安装并成功启动，Info.plist 核对为 `2.2.2 / 91`；Card Content Editor 视觉矩阵、Apple Photos 分享/保存回读仍需人工操作完成，不使用 Simulator 替代。

## 6. 授权与停止点

- [x] 已按实际最近一次 GitHub 推送确定基线 `916fc84`。
- [x] 已明确保持 marketing version `2.2.2`，构建号更新为 `91`。
- [x] 用户已授权合并到 `main` 并推送 GitHub。
- [ ] TestFlight 上传仍需独立授权。
- [ ] App Store Connect / App Store 提交仍需独立授权。
- [ ] P0 `TX-001`、`BP-001` 和 superseding production certification 不因本次源码同步自动关闭。
