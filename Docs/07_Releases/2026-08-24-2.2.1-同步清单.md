# MemoMark 2.2.1 (87) 同步清单

## 1. 同步状态与决策门

- Primary loop：Engineering Loop（P1 媒体真实性与交接可靠性）和 Product Loop（P2 配置中心表达可理解性）保持为独立问题；本版本只汇总两条已完成的受控改进，不改变其所有权。
- 范围起点：已推送 `2.1.3 (86)`，基线提交 `a438e55`，2026-08-19。
- 范围终点：2026-08-24 当前本地工作树的可归属变更。
- marketing version：`2.2.1`；build number：`87`。
- 风险：P1（Apple Photos/Live Photo 保存、配对与交接）；P2（表达样式与 Configuration Center 可理解性）。原图保护、照片本地处理、Memory Engine、Layout Engine、Renderer 与 Share Extension 所有权均不改变。
- Apple-native 复用：Photos/PhotoKit 仍负责资源身份、Live Photo 配对与保存；MemoMark 只处理 intake、解析、渲染、交接与回读验证。
- 状态：`Source Checkpoint Ready; Release Evidence Open; External Distribution Not Authorized`。
- Git 授权：项目负责人于 2026-08-24 明确授权按本清单执行 Git 暂存、提交和推送至 `main -> origin/main`。
- 未授权：TestFlight 上传与 App Store 提交；两项外部交付仍需单独授权。

## 2. 工作树分类

### 正式同步范围（待验证和授权后暂存）

- Minimal/Classic White 表达样式、Presentation Artifact、Layout/Renderer 路由、配置持久化与预览一致性。
- Share Extension 到主 App 的 Live Photo provider handoff、真实身份恢复、still/video composition、PhotoKit readback、可恢复失败与不重复处理。
- 时间锚点年度生日语义、配置中心的表达/卡片样式语言收口、四语本地化和相关测试。
- `2.2.1 (87)` target 版本字段、应用内更新日志、CHANGELOG、README、CURRENT_STATUS、HANDOFF、发布目录索引和本整理包。

### 本地保留范围（不得暂存）

- DerivedData、`.xcresult`、签名或归档产物、设备容器、截图/录屏、私人照片及研究数据。
- `.autosync/`、`.codex/`、个人 Xcode 状态和未冻结研究实验资产。
- `2026-08-22-2.1.4-*` 四份本地 Draft：它们在 build 未锁定时形成，已被本 `2.2.1 (87)` 包取代，不属于本次建议暂存范围。

### 待决定范围

- Dynamic Type、长文本、各语言视觉复核、其他高成本媒体矩阵、StoreKit 和 App Store 外部证据的完成时机。

## 3. 统一事实源分发

| 受众 | 文件 | 内容边界 |
| --- | --- | --- |
| 应用内用户 | 四个 `Localizable.strings` 中的 `settings.release_notes.*` | 简洁、温和地说明卡片样式、动态照片、本地处理与原图保护。 |
| App Store | `2026-08-24-2.2.1-AppStore更新说明.md` | 最短用户价值与隐私边界；不含测试术语或内部名称。 |
| TestFlight | `2026-08-24-2.2.1-TestFlight测试说明.md` | 可执行的样式、Live Photo、时间语义、分享与反馈路径。 |
| 内部同步 | 本文件与 `2026-08-24-2.2.1-版本更新说明.md` | 范围、风险、证据、排除项、授权与停止点。 |

## 4. 验证矩阵

| 项目 | 当前证据 | 状态 |
| --- | --- | --- |
| 版本字段一致性 | `project.pbxproj` 中 12 个 build 字段均为 `87`，10 个 marketing 字段均为 `2.2.1`；覆盖主 App、macOS、Share Extension、Widget、Tests 与 Device QA | 通过静态核对 |
| 发布说明与四语资源 | 四份 `2.2.1 (87)` 材料存在；四个 `Localizable.strings` 均通过 `plutil -lint` | 通过静态核对 |
| `git diff --check` | 本轮材料与版本字段更新后已运行 | 通过 |
| 聚焦测试 | `V1ReleaseNotesContractTests` 在 2026-08-24 以 4 项断言通过；本轮补救还复核了年度文案、设置帮助、Classic White 输出边缘与 iPhone 自适应布局测试 | 通过 |
| 全量 macOS 测试 | `xcodebuild test` 在当前 `2.2.1 (87)` 工作树串行运行，命令退出码 `0`；结果包：`/tmp/PhotoMemo221FullSerial.xcresult` | 通过 |
| macOS Debug 候选构建 | `PhotoMemo.app` 已生成；产物 `Info.plist` 为 `2.2.1 (87)` | 产物版本已核验；完整构建日志证据待独立留存 |
| 实体 Live Photo round-trip | 8 月 23 日当前工作树已完成 Share、保存、回读、播放与重启不重复处理复核 | 已有设备证据；不替代新候选构建复核 |
| 视觉/可访问性/外部交付 | Dynamic Type、长文本、各语言视觉、StoreKit、TestFlight、App Store Connect | 未关闭 |

## 5. 同步前检查表

- [x] 已确定版本范围起点 `a438e55`、marketing version `2.2.1` 和顺延 build `87`。
- [x] 已区分正式同步、本地保留和待决定范围；2.1.4 Draft 明确排除。
- [x] 已建立版本总表、App Store、TestFlight 与内部同步四份材料。
- [x] 已验证 App、Share Extension、Widget、macOS、test target 与 Device QA 的版本字段全部一致。
- [x] 已核对应用内、App Store、TestFlight、CHANGELOG、README 与 CURRENT_STATUS 的版本号、构建号和发布边界一致。
- [x] 已完成四语资源格式检查、键集合 parity、`git diff --check` 与候选产物版本核对。
- [x] 已完成当前工作树的聚焦测试、完整串行 macOS 测试与候选构建。自动化、设备、StoreKit 和外部平台证据继续分开记录。
- [x] 已核对最终 `git status` 和建议暂存清单；四份 `2.1.4 Draft` 仍仅在工作树中，未进入同步范围。
- [x] 已获得 Git 暂存、提交和推送授权；同步仅限 `main -> origin/main`。
- [ ] 未获得 TestFlight 上传或 App Store 提交授权；不得随 Git 同步一并执行。

## 6. 停止点

当前状态允许一个经过验证的 GitHub 源码检查点；任何新的测试或构建失败都会保持现场，并回到 `Release Evidence Open`。TestFlight 与 App Store 状态仍不得随 Git 同步变更。
