# MemoMark 2.3.3（109）同步清单

状态：`Version Locked; Release Evidence Open`

## 1. 版本总表

- 项目：`MemoMark`
- 仓库：`/Users/rui/Desktop/PhotoMemo`
- 范围起点：2026-09-07，`2.3.0 (105)`
- 上一个已记录候选：`2.3.2 (108)`
- 当前 marketing version：`2.3.3`
- 当前 build number：`109`
- 当前源码基线：`935f5b29`
- 当前分支：`main`
- `2.3.1`：没有可独立确认的候选材料，不创建虚构的构建号或商店状态
- 当前目标：完成本地版本材料、版本字段、自动化验证和指定 iPhone 17 Pro Max 初步检查；等待后续 GitHub/TestFlight/App Store 授权

## 2. 同一事实源与范围摘要

本轮统一汇总 9 月 7 日以来的连续维护，不把历史候选误写成已发布版本：

- `2.3.0 (105)`：Classic White/Minimal 内容边界、Minimal 横屏预览、macOS Configuration Center、MemoMark+ 四语权益文案。
- 9 月 10–18 日：配置中心跨平台收口、FilmMark 独立内容/外观/布局关系、预览与输出合同、四语和契约测试维护。
- 9 月 20 日：当前配置与处理默认边界修复、无法解析时失败关闭、FilmMark 生产快照完整性和本地配置/队列保护。
- `2.3.2 (108)` 候选：样式选择与实际输出一致性、保存状态、较大文字选择路径和配置中心辅助层级。
- 9 月 22 日当前 HEAD：源码健康化、PhotoKit/FilmMark 生产路径契约、四语资源与无障碍布局契约收口；仍不等于真机或生产认证完成。

## 3. 四套发布材料

- `2026-09-22-2.3.3-release-notes.md`：应用内完整说明与内部边界投影。
- `2026-09-22-2.3.3-app-store-whats-new.md`：App Store 简体中文和 U.S. English 文案。
- `2026-09-22-2.3.3-testflight-notes.md`：测试路径、反馈字段和隐私提醒。
- `2026-09-22-2.3.3-sync-manifest.md`：本文件，记录范围、证据、风险和停止点。

同时维护：`CHANGELOG.md`、`README.md`、`README_EN.md`、`Docs/CURRENT_BRIEF.md`、`Docs/CURRENT_STATUS.md`、四语 `Localizable.strings` 和所有目标版本字段。

## 4. 正式同步范围

- MemoMark App、iOS App、Share Extension、Widget Extension、Device QA 和测试 target 的 `2.3.3 / 109` 版本字段。
- 当前已接受的配置中心、FilmMark、PhotoKit/队列可靠性、四语资源、测试保护和发布记录。
- 本轮版本材料、CHANGELOG、README、CURRENT_BRIEF、CURRENT_STATUS 和应用内更新日志。
- 已确认不需要的根目录占位、空文档、空 Swift 文件和未引用历史图标清理。

## 5. 本地保留与排除

- `Docs/Outreach/`：本地保留，不暂存、不提交、不推送。
- 私人照片、设备截图、归档包、签名产物、DerivedData、Xcode 用户状态和 `/tmp` 诊断：不进入仓库。
- App Store Connect、TestFlight、Xcode Cloud 和 App Review 页面状态：不由本地文件或构建结果推断。
- TX-001、BP-001、真机媒体读回、StoreKit 和生产认证：保持独立证据门，不在材料中写成已关闭。

## 6. 验证清单

- [x] 范围起点、上一候选、marketing version `2.3.3` 和 build `109` 已明确。
- [x] App、Share Extension、Widget、Device QA 和测试 target 的工程版本字段已统一为 `2.3.3 / 109`；指定真机上的主 App `com.serydoo.PhotoMemo.iOS` 已由 `devicectl` 读回为 `2.3.3 / 109`。
- [x] 应用内、App Store、TestFlight 和内部材料使用同一事实源；应用内四语文案已更新。
- [x] 四语资源格式/键集合、版本资料契约和聚焦测试在本次版本字段更新后通过；完整 `MemoMarkTests` 为 `1905 passed / 0 failed / 1 skipped`，结果包为 `/tmp/MemoMark233Full/Logs/Test/Test-MemoMarkTests-2026.09.22_21-31-12-+0800.xcresult`。结果仍记录两条既有 `FixtureExportReadbackTests` QoS priority-inversion runtime warning。
- [x] macOS Debug 构建在 `2.3.3 / 109` 下通过；指定 iPhone 目标的签名 `MemoMarkiOS` 构建也通过。
- [x] 签名 iPhone 17 Pro Max `2.3.3 (109)` 已完成同 Bundle ID 覆盖安装和版本读回，未卸载或清除 App 容器；`codesign --verify --deep --strict` 通过。
- [ ] 用户初步真机检查：更新日志、Configuration Center、Classic White/Minimal/FilmMark、保存重开、基础无障碍和 Apple Photos 初步路径。
- [x] 解锁后 `devicectl` 启动成功，并观察到 `MemoMarkiOS` 与 `MemoMarkWidgetExtension` 进程；这只关闭构建/安装/启动证据，不关闭用户手工验收。
- [ ] Apple Photos/Share/Live Photo 完整读回、StoreKit、性能、TX-001、BP-001 和 production certification：仍开放。
- [ ] GitHub commit/push：等待用户后续明确指令；本轮不执行。
- [ ] TestFlight 上传、App Store Connect 修改/提交：等待单独授权；本轮不执行。

## 7. 当前停止点

本候选已完成本地材料、构建、覆盖安装和解锁后启动，现交由用户检查 iPhone 17 Pro Max。启动成功不等于逐屏 UI、VoiceOver、Photos、Share、Live Photo、StoreKit 或生产认证完成；在用户反馈前保持 `Release Evidence Open`。不得因为当前浏览器打开 App Store Connect 就视为获得外部操作授权。
