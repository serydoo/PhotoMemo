# MemoMark 2.3.3（111）同步清单

状态：`Version Locked; Release Evidence Open`

## 1. 版本总表

- 项目：`MemoMark`
- 仓库：`/Users/rui/Desktop/PhotoMemo`
- 范围起点：2026-09-07，`2.3.0 (105)`
- 上一个已记录候选：`2.3.2 (108)`
- 当前 marketing version：`2.3.3`
- 当前 build number：`111`
- 当前源码身份：基于 `e752f49f` 的当前未提交工作树；最终同步前需要形成唯一 commit
- 当前分支：`main`
- `2.3.1`：没有可独立确认的候选材料，不创建虚构的构建号或商店状态
- 当前目标：完成本地版本材料、版本字段、自动化验证和本版本 FilmMark/TX-001/BP-001 初步关闭；完成其他优化后形成可追溯 commit，等待后续 GitHub/TestFlight/App Store 授权

## 2. 同一事实源与范围摘要

本轮统一汇总 9 月 7 日以来的连续维护，不把历史候选误写成已发布版本：

- `2.3.0 (105)`：Classic White/Minimal 内容边界、Minimal 横屏预览、macOS Configuration Center、MemoMark+ 四语权益文案。
- 9 月 10–18 日：配置中心跨平台收口、FilmMark 独立内容/外观/布局关系、预览与输出合同、四语和契约测试维护。
- 9 月 20 日：当前配置与处理默认边界修复、无法解析时失败关闭、FilmMark 生产快照完整性和本地配置/队列保护。
- `2.3.2 (108)` 候选：样式选择与实际输出一致性、保存状态、较大文字选择路径和配置中心辅助层级。
- `2.3.3 (111)` 当前候选：三种展示框架和 FilmMark 自定义空间、配置中心关系重组、记忆对象与时间锚点编辑收口，以及样式选择与实际输出一致性、本地处理边界维护；本版本另记录 FilmMark/TX-001/BP-001 的初步关闭。
- 9 月 22 日当前 HEAD：源码健康化、PhotoKit/FilmMark 生产路径契约、四语资源与无障碍布局契约收口；指定 FilmMark 版本范围已有初步关闭记录，但不等于外部提交或全局永久认证。

## 3. 四套发布材料

- `2026-09-22-2.3.3-release-notes.md`：应用内完整说明与内部边界投影。
- `2026-09-22-2.3.3-app-store-whats-new.md`：App Store 简体中文和 U.S. English 文案。
- `2026-09-22-2.3.3-testflight-notes.md`：测试路径、反馈字段和隐私提醒。
- `2026-09-22-2.3.3-sync-manifest.md`：本文件，记录范围、证据、风险和停止点。

同时维护：`CHANGELOG.md`、`README.md`、`README_EN.md`、`Docs/CURRENT_BRIEF.md`、`Docs/CURRENT_STATUS.md`、四语 `Localizable.strings` 和所有目标版本字段。

## 4. 正式同步范围

- MemoMark App、iOS App、Share Extension、Widget Extension、Device QA 和测试 target 的 `2.3.3 / 111` 版本字段。
- 当前已接受的配置中心、FilmMark、PhotoKit/队列可靠性、四语资源、测试保护和发布记录。
- 本轮版本材料、CHANGELOG、README、CURRENT_BRIEF、CURRENT_STATUS 和应用内更新日志。
- 已确认不需要的根目录占位、空文档、空 Swift 文件和未引用历史图标清理。

## 5. 本地保留与排除

- `Docs/Outreach/`：本地保留，不暂存、不提交、不推送。
- 私人照片、设备截图、归档包、签名产物、DerivedData、Xcode 用户状态和 `/tmp` 诊断：不进入仓库。
- App Store Connect、TestFlight、Xcode Cloud 和 App Review 页面状态：不由本地文件或构建结果推断。
- `2.3.3 (111)` 的 TX-001、BP-001 和指定 iPhone 17 Pro Max FilmMark 全链路：按产品负责人决定已初步关闭；后续反馈或新证据可重新打开。StoreKit 商品/购买/恢复/权益兼容逻辑本轮未触动，不新增 sandbox 结论。

## 6. 验证清单

- [x] 范围起点、上一候选、marketing version `2.3.3` 和 build `111` 已明确。
- [x] App、Share Extension、Widget、Device QA 和测试 target 的工程版本字段已统一为 `2.3.3 / 111`；指定真机上的主 App `com.serydoo.PhotoMemo.iOS` 已读回本轮新构建版本。
- [x] 应用内、App Store、TestFlight 和内部材料使用同一事实源；应用内四语文案已更新并突出三种展示框架、FilmMark 自定义空间和记忆对象整理。
- [x] 四语资源格式/键集合、版本资料契约和聚焦测试在本次版本字段更新后通过；修正本地化契约后完整 `MemoMarkTests` 为 `1909 passed / 0 failed / 1 skipped`，结果包为 `/tmp/MemoMark233Full111Final/Logs/Test/Test-MemoMarkTests-2026.09.23_10-54-43-+0800.xcresult`。结果仍记录两条既有 `FixtureExportReadbackTests` QoS priority-inversion runtime warning，以及 Xcode beta 的 SwiftCompile 诊断输出。
- [x] 指定 iPhone 目标的签名 `MemoMarkiOS` Debug 构建、包签名校验和 `Info.plist` 版本读回通过，均为 `2.3.3 / 111`。
- [x] 签名 iPhone 17 Pro Max 已按同 Bundle ID 覆盖安装并读回 `2.3.3 (111)`，继续未卸载或清除 App 容器数据。
- [x] 产品负责人已记录 `2.3.3 (111)` 范围内 FilmMark 全链路、`BP-001` 和 `TX-001` 的初步关闭；记录见 `Docs/01_Product/V4_FilmMark_2.3.3_111_Initial_Production_Closure_2026-09-23.md`。
- [x] 时间锚点 VoiceOver 动作、顺序提示和类型前缀完成四语资源化，活动本地化审计与资源奇偶性 focused 测试通过。
- [ ] 用户初步真机检查：更新日志、Configuration Center、Classic White/Minimal、保存重开、基础无障碍和 Apple Photos 初步路径；FilmMark 指定链路已按本版本范围初步关闭。
- [x] 解锁后 `devicectl` 启动 `2.3.3 (111)` 成功；该启动证据对应本轮新构建。
- [ ] 其他 app-wide Apple Photos/Share/Live Photo 完整读回、StoreKit、性能和外部 production certification：仍按独立证据边界处理；FilmMark 指定链路、TX-001、BP-001 已按本版本范围初步关闭。
- [ ] GitHub commit/push：等待用户后续明确指令；本轮不执行。
- [ ] TestFlight 上传、App Store Connect 修改/提交：等待单独授权；本轮不执行。

## 7. 当前停止点

本候选已完成本地材料、全量测试、签名构建、包与设备版本读回、覆盖安装和解锁后启动；`2.3.3 (111)` 的 FilmMark 指定链路、TX-001、BP-001 已按产品决定初步关闭，后续反馈可重新打开。其他逐屏 UI、VoiceOver、Photos、Share、Live Photo、StoreKit 与外部提交仍按独立证据边界处理；在 GitHub 同步和外部授权前保持 `Release Evidence Open`。不得因为当前浏览器打开 App Store Connect 就视为获得外部操作授权。
