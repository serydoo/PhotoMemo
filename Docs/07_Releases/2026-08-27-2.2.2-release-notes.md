# MemoMark 2.2.2 (91) 版本更新说明

## 版本与范围

- 版本：`2.2.2`
- 构建号：`91`
- 最近一次 GitHub 推送基线：`916fc843680181dbfe1a619e05bfc9c7e38aa027`
- 对比范围：`916fc84..current working tree`
- 当前状态：源码已授权同步 GitHub；TestFlight、App Store 与生产认证仍独立开放。

本构建是 2.2.2 的维护收口，保留构建 90 的历史材料和所有已发布兼容值，集中完成
Card Content Editor 输入几何标准、TextKit 混排稳定性和本轮可靠性审查的工程化落地。

## 本轮变化

- 普通文字、模块 attachment 和原生 caret 各自拥有明确 geometry owner，并共享一个不受内容顺序影响的 canonical line box。
- 普通文字使用字体度量推导的正向 half-leading；attachment 不继承普通文字的 baseline offset；模块前、后、夹中、删除和重新插入均不再改变文字垂直位置。
- 将 40pt 输入面、28pt 行盒、6pt 标题列和 16pt caret 的关系固化为可复用规范，覆盖 IME、Dynamic Type、辅助功能字号和实体设备验收边界。
- 保留本轮队列、PhotoKit、Live Photo、路径和本地化可靠性修复，以及原始照片不变、本地处理和 Apple Photos 所有权边界。
- 新增几何和源契约测试，未来其他 Renderer 编辑器必须先复用规范，不得复制局部补偿或把输入布局责任下沉到 Renderer。

## 验证与未关闭证据

验证结果：`xcodebuild -list` 通过；macOS、通用 iOS、Share Extension、Widget Extension 和 Device QA 构建通过；全量 `MemoMarkTests` 为 `1603 passed / 0 failed / 1 skipped`；Python 脚本测试 `117` 项通过；资源 lint、Shell 语法、`git diff --check` 与版本字段核对通过。91 号签名包已在配对实体 iPhone 17 Pro Max 上覆盖安装并成功启动，设备端核对为 `2.2.2 / 91`；Card Content Editor 人工视觉矩阵、Apple Photos 分享/保存回读、TestFlight、App Store Connect、`TX-001`、`BP-001` 与 superseding production certification 仍不因本构建自动关闭。

## 不变的原则

照片只在设备本地处理，原始照片不被修改；Apple Photos 继续拥有照片库身份和最终保存事务。Memory Engine、Layout Engine、Renderer、Export、Share Extension 和持久化配置的职责边界不变。
