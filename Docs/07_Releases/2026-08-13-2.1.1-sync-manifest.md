# MemoMark 2.1.1 (80) 同步清单

## 基线与目标

- 用户指定范围起点：2026-08-09（周日）约 14:12，Asia/Shanghai。
- Git 相邻基线：`588792d`（2026-08-09 14:06:59，`2.1.0 (76)` 源码检查点）。
- 基线之后已同步的历史检查点：`150bfb5`、`6cff024`、`382fc1a`、`e4a50ce`、`1cc94ed`。
- 此前的 `2.1.1 (77)` 和 `2.1.2 (79)` 保留为未上架历史源码候选；不覆盖历史 release materials。
- 本轮目标：`2.1.1 (80)`。
- 目标分支：当前 `main`；本轮是用户明确授权的 GitHub 源码同步，不创建 TestFlight 或 App Store 外部交付动作。
- 状态：`Source Checkpoint Ready; Store Delivery Evidence Open`。

## 正式同步范围

- 卡片内容四区域、记忆对象、配置和输出设置的上下文连续性；
- Apple mini Logo、Subject Avatar 与 custom-upload Logo 的来源和归属分离；
- Logo 选择、圆形预览、最终输出、保存、重新加载、对象/配置切换的一致性；
- Logo 资源在导入、备份恢复、历史迁移、运行时路径转换和缺失回退中的一致性；
- 新 Logo 选择使旧异步结果失效，避免 late completion 覆盖用户选择；
- 相关架构回归测试、聚焦测试、双语应用内更新日志、App Store 文案、TestFlight 说明；
- `CHANGELOG.md`、`README.md`、`Docs/CURRENT_STATUS.md`、`Docs/07_Releases/README.md` 和项目所有版本字段。

## 排除范围

- 私人照片、人物与地点数据、真机容器、日志、截图和录屏；
- 签名构建、DerivedData、归档包、个人 Xcode 状态和设备安装产物；
- 未冻结 V4 研究、未验证的生产声明、`TX-001`、`BP-001` 的关闭结论；
- TestFlight 上传、App Store 提交、商店截图和 App Store Connect 版本训练操作；
- 与本轮 Logo/配置闭环无关的本地实验或未来产品设计。

## 同步前验证门槛

- [x] 当前 Logo/配置聚焦回归：`249/249`，0 failures，0 skips；
- [x] 版本更新后的 macOS Debug、通用 iOS Debug、签名 iOS Debug 构建，以及连接设备原位安装和启动已完成；设备显示 `2.1.1 (80)`；
- [x] `git diff --check` 已通过；
- [x] 版本改为 `2.1.1 (80)` 后重新运行 release-note contract、受影响回归和 macOS/iOS 构建；
- [x] 暂存前确认只包含正式同步范围；
- [x] 已提交 `730b0af`，并核对 commit、远端 `origin/main` 和工作树；
- [ ] TestFlight/App Store 仍需独立授权和独立证据。

## 版本事实源分发

| 受众 | 文件 | 规则 |
| --- | --- | --- |
| 应用内用户 | `Source/PhotoMemo/PhotoMemo/zh-Hans.lproj/Localizable.strings`、`en.lproj/Localizable.strings` | 完整、克制、说明卡片内容与 Logo 体验；不出现内部测试或 Git 信息 |
| App Store | 本目录 `2026-08-13-2.1.1-app-store-whats-new.md` | 只保留用户价值、本地处理和原图不变 |
| TestFlight | 本目录 `2026-08-13-2.1.1-testflight-notes.md` | 提供可执行路径、反馈格式和明确边界 |
| 内部同步 | 本文件、发布总表、`CHANGELOG.md`、`Docs/CURRENT_STATUS.md` | 记录基线、版本训练风险、证据和未完成事项 |

## 源码同步记录

- 用户已授权整理、暂存、提交和推送 GitHub。
- 本轮应先完成版本字段变更后的验证，再提交到当前 `main`。
- 已完成推送：`730b0af`（`Sync MemoMark 2.1.1 build 80 Logo closure`）已进入 `main`；`main`、`origin/main` 和 GitHub `refs/heads/main` 均指向同一提交，工作树干净。
- 源码同步成功只关闭 GitHub 源码检查点；不关闭人工验收、Apple Photos、StoreKit、TestFlight、App Store 或生产认证边界。
