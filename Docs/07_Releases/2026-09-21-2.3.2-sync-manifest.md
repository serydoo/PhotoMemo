# MemoMark 2.3.2（108）同步清单

状态：`Version Locked; Release Evidence Open`

## 2026-09-22 当前 HEAD 增量

- 当前 HEAD 为 `935f5b29`；配置事务、FilmMark/PhotoKit 生产路径、四语本地化和模拟器无障碍布局契约的健康化已进入源码基线。
- 当前 HEAD 重新执行完整 `MemoMarkTests`：`1905 passed / 0 failed / 1 skipped`，结果为 `Passed`；治理检查和 `git diff --check` 通过。
- 以上只关闭源码/契约/自动化层问题。实体 iPhone 17 Pro Max 手工矩阵、Apple Photos/Share/Live Photo 读回、TX-001、BP-001、性能测量和 superseding production certification 仍开放。
- 最新总审查与关闭口径见 `Docs/03_Engineering/2026-09-22-comprehensive-audit-closure-and-current-state.md`。

## 1. 范围锚点与版本口径

- 项目：`MemoMark`
- 仓库目录：`/Users/rui/Desktop/PhotoMemo`
- 变更范围起点：用户指定的 `2.3.1`
- 当前 marketing version：`2.3.2`
- 当前 build number：`108`
- 当前分支：`main`
- 本轮目标：准备本地源码、版本资料和 TestFlight 测试说明；不执行外部同步。
- 2.3.1 只作为本轮用户指定的版本更新起点记录；仓库中没有可独立确认的 2.3.1 正式候选构建，因此不填充不存在的构建号或商店状态。

## 2. 同一事实源

本轮事实源首先是 FM（FilmMark／胶片时间）的持续开发，其次是配置中心样式选择与处理默认之间的边界修复，以及针对截图发现的配置中心 UI 规范修复。

- FM 仍处于持续开发和 TestFlight 体验阶段；本版本只准备开发体验资料，不宣称 FM 已完成生产认证。
- 当前预设选择通过当前配置 ID 构造不可变处理快照，无法解析时失败关闭，不静默回退经典白。
- 卡片样式、时间表达和配置说明使用统一的辅助文字层级。
- 卡片样式选择与时间表达选择使用相同的横向空间适配策略。
- 保存主按钮使用项目按钮字级；原图、本地处理和 Apple Photos 回写边界保持不变。

## 3. 正式同步候选

- 配置中心源码和对应架构回归测试。
- 四语言 `Localizable.strings` 中的应用内版本更新说明。
- `Source/MemoMark/MemoMark.xcodeproj/project.pbxproj` 中所有 App、Share Extension、Widget Extension、Device QA 和测试 target 版本字段。
- `CHANGELOG.md`、`README.md`、`Docs/CURRENT_STATUS.md`。
- 本版本四份材料：
  - `2026-09-21-2.3.2-release-notes.md`
  - `2026-09-21-2.3.2-app-store-whats-new.md`
  - `2026-09-21-2.3.2-testflight-notes.md`
  - `2026-09-21-2.3.2-sync-manifest.md`

## 4. 本地保留与排除

- `Docs/Outreach/` 及其未冻结的对外触达材料：不进入本轮同步候选。
- 私人照片、设备截图、归档包、签名产物、DerivedData、Xcode 用户状态和 `/tmp` 下诊断：不进入仓库。
- 任何无法证明属于本轮范围的研究、实验资产或外部发布材料：暂不纳入。

## 5. 验证清单

- [x] 已确定版本起点 `2.3.1`、marketing version `2.3.2` 和 build `108`。
- [x] 所有 12 个 `CURRENT_PROJECT_VERSION` 为 `108`。
- [x] 所有 10 个 `MARKETING_VERSION` 为 `2.3.2`。
- [x] 应用内四语言版本说明已对齐本轮事实源。
- [x] 新增配置中心 UI 契约测试，先失败后通过。
- [x] `git diff --check`：最终检查通过；治理检查 `python3 scripts/validate_codex_governance.py .` 同样通过。
- [x] 四语言键集合/格式检查：`LocalizationResourceParityTests` 与版本资料契约通过，四语资源保持键集合和本轮版本说明一致。
- [x] 完整 `MemoMarkTests`：使用全新 DerivedData 执行，xcodebuild 退出码为 0；Xcode beta 仍输出若干 `SwiftCompile ... exit code 0 but produced no further output` 工具链诊断和既有 API 弃用警告，未出现测试失败。
- [x] macOS Debug 构建：通过；仅有 `CLGeocoder` 已弃用警告。
- [x] 通用 iOS / 签名 iPhone 17 Pro Max 构建与覆盖安装：均通过；签名校验通过，覆盖安装保留既有 App 容器，设备查询确认安装版本为 `2.3.2 (108)`；安装不等同于手工验收。
- [ ] 真机视觉、Dynamic Type、VoiceOver、Photos、Share、Live Photo：待手工执行。
- [ ] TestFlight 归档上传、App Store Connect 和生产认证：未执行。

## 6. 已完成的本地证据

- 配置中心 UI 契约、版本资料契约和四语资源契约均通过；新增 UI 契约遵循先失败后通过的 TDD 过程。
- macOS `MemoMark` Debug 构建、通用 `MemoMarkiOS` iPhoneOS Debug 构建均通过。
- 指定物理设备 `iPhone 17 Pro Max`（`863C2747-6742-5E93-B715-6F89DBF90B31`）完成签名构建、深度签名校验和同 Bundle ID 覆盖安装；`devicectl` 查询为 `MemoMark 2.3.2 108`。
- 设备启动请求被系统以 `Locked` 拒绝，因此本轮没有把逐屏视觉、无障碍、Photos、Share 或 Live Photo 手工验收标记为完成；没有因锁定状态卸载、清容器或切换设备。

## 7. 授权与停止点

本轮没有执行 `git add`、commit、push、TestFlight 上传或 App Store Connect 修改。后续如需 GitHub 同步，
必须先按逐文件清单获得暂存、提交和推送授权；TestFlight 上传和 App Store 提交仍需分别授权。
