# MemoMark 2.2.2 (99) 同步清单

## 1. 版本与基线

- Marketing version：`2.2.2`
- Build number：`99`
- 正式版范围起点：2026-08-24（周一）约 06:54
- 起点对齐的最近已推送提交：`c0f11a7aa36b7a3b0107b3e6003214e57376e074`（06:49:40 +0800）
- 本轮同步范围：`c0f11a7..HEAD`；包含上一轮 TestFlight 已交付的用户功能与修复
- 当前状态：App Store 正式版候选；上一版 `2.2.1 (87)` TestFlight 内容已纳入，正式提交与上线证据仍开放
- 当前工程：`/Users/rui/Desktop/PhotoMemo/Source/MemoMark/MemoMark.xcodeproj`

## 2. 正式同步范围

- `Source/MemoMark/` 当前源码、四语资源、工程文件、target、scheme 和 plist。
- `Tests/MemoMarkTests/` 当前测试与 source-contract 覆盖。
- `AGENTS.md`、`CHANGELOG.md`、`README.md`、`README_EN.md`、`HANDOFF.md`、`Docs/CURRENT_STATUS.md`。
- `Docs/03_Engineering/` 本轮输入几何规范、编辑器 UI pass 和审查修复记录。
- 本目录的五份 `2026-08-29-2.2.2-*` 版本材料；旧 2026-08-26 构建 90 材料保留历史事实。

## 3. 本地保留范围

私人照片、设备截图、诊断导出、归档包、签名产物、DerivedData、`.xcresult`、`output/` 历史实验资产、Xcode 用户状态和其他未纳入工程的本地材料不进入提交。

## 4. 版本字段与兼容值

- `project.pbxproj` 中所有 `MARKETING_VERSION` 应为 `2.2.2`。
- `project.pbxproj` 中所有 `CURRENT_PROJECT_VERSION` 应为 `99`，覆盖 App、iOS、Share Extension、Widget、测试和 Device QA。
- `com.serydoo.PhotoMemo`、`com.serydoo.PhotoMemo.iOS`、App Group、`photomemo.*`、后台任务、StoreKit 商品 ID 和旧 `photomemo://` 解析能力保持不变。
- Settings 版本文案从 Bundle 读取，不硬编码构建号。

## 5. 验证记录

- [x] `xcodebuild -list`：通过，6 个方案均可解析。
- [x] Xcode Cloud 构建 94 根因已复现为 `PhotoKitResourceFileName.swift:10` 的 `PHAssetResource.filename` 兼容性错误；源码已改为稳定的 `originalFilename` KVC 读取，不再依赖新 SDK 成员。
- [x] macOS `MemoMark` Debug build：generic macOS 目的地通过；99 号版本字段已进入构建。
- [x] 通用 iOS `MemoMarkiOS` Debug 构建通过，产物版本为 `2.2.2 (99)`；Xcode Cloud Archive 将由本次 `main` 推送触发。
- [x] `MemoMarkShareExtension` build：generic iOS 目的地通过，99 号版本字段已进入构建。
- [x] `MemoMarkWidgetExtension` build：generic iOS 目的地通过，99 号版本字段已进入构建。
- [x] `MemoMarkDeviceQA` build：generic iOS 目的地通过，99 号版本字段已进入构建。
- [x] 当前 `MemoMarkTests` 全量测试：`1611 passed / 0 failed / 1 skipped`，结果 `Passed`；含参数化执行共 `1634` 次通过，发现 2 条既有 QoS runtime warning，未产生失败。
- [x] 几何、编辑器 source-contract 和发布契约均包含在全量通过结果内；此前独立几何 focused run 仍为 8/8，通过。
- [x] Python 脚本测试：`117` 项通过；Shell 脚本语法检查通过；4 个本地化资源与 Share Extension Info.plist lint 通过。
- [ ] `git diff --check`、版本字段静态核对、未跟踪/私有产物审查：待本轮最终验证；目标为 12 个 `CURRENT_PROJECT_VERSION` 为 99、10 个 `MARKETING_VERSION` 为 2.2.2。
- [ ] 99 号实体 iPhone 17 Pro Max 人工视觉与 Apple Photos 全链路：按用户最新指令本轮跳过，不能以 Debug 构建或旧设备证据替代。

## 6. 授权与停止点

- [x] 已按用户指定的 2026-08-24 约 06:54 推送波次确定基线；因无 06:54 独立提交，对齐最近已推送的 `c0f11a7`（06:49:40）。
- [x] 已明确保持 marketing version `2.2.2`，构建号更新为 `99`。
- [x] 用户已授权合并到 `main` 并推送 GitHub。
- [x] 用户已授权 GitHub 同步后由 Xcode Cloud 自动构建并进入 TestFlight 流程；处理结果仍需 App Store Connect 证据。
- [ ] App Store Connect / App Store 提交仍需独立授权。
- [ ] P0 `TX-001`、`BP-001` 和 superseding production certification 不因本次源码同步自动关闭。
