# MemoMark 2.2.4 (101) 同步清单

## 1. 版本与基线

- Marketing version：`2.2.4`
- Build number：`101`
- 基线：`0954bea`（`main` 与 `origin/main` 当前共同提交）
- 同步候选范围：`0954bea..当前工作树`
- 当前状态：`Version Locked; Manual Acceptance Complete; Engineering Follow-ups Deferred`。
- 当前工程：`/Users/rui/Desktop/PhotoMemo/Source/MemoMark/MemoMark.xcodeproj`

本轮是行为保持的核心架构现代化与验证整理。当前工作树已位于 `main`，
不存在另一个待合并分支；源码候选已提交并推送到 GitHub，提交为 `ac95ab3`。

## 2. 正式同步范围

- `Source/MemoMark/`：应用事务、领域/基础设施边界、队列与 PhotoKit
  持久化恢复、Share intake、配置中心拆分、四语资源及工程配置。
- `Tests/MemoMarkTests/` 与 `Tests/MemoMarkUITests/`：迁移后的责任边界、
  媒体/队列/快照合同和 Device QA 覆盖。
- `Docs/02_Architecture/`、`Docs/03_Engineering/`、`Docs/ADR/`：RFC-002、
  ADR-011、命名迁移、根视图责任图和工程整理记录。
- `QA/README.md`、`AGENTS.md`、`CHANGELOG.md`、`README.md`、
  `Docs/CURRENT_BRIEF.md`、`Docs/CURRENT_STATUS.md`、`HANDOFF.md`。
- 本目录 `2026-09-03-2.2.4-*` 版本材料及历史版本材料。

## 3. 本地保留范围

- 私人照片、设备截图、诊断导出、归档包、签名产物、DerivedData、
  `.xcresult`、本地输出相册与不在工程中的实验资产。
- `.codex/config.toml` 及 `.codex/skills/` 下的个人工具配置与本地技能。
- 任意新出现但不在上述正式范围内的设备资料、个人数据或生成产物。

已清理的可再生本地缓存：`scripts/__pycache__/` 与
`scripts/tests/__pycache__/`。它们不属于同步范围。

## 4. 版本字段与兼容边界

- `project.pbxproj` 应有 **10** 个 `MARKETING_VERSION = 2.2.4`。
- `project.pbxproj` 应有 **12** 个 `CURRENT_PROJECT_VERSION = 101`，覆盖
  macOS App、iOS App、Share Extension、Widget、测试与 Device QA。
- Bundle ID、App Group、URL scheme、后台任务标识、StoreKit 商品 ID、
  持久化键和历史 schema 保持不变。
- 活跃产品路径不再使用阶段式 `V1` 名称；剩余 `V1` typealias 仅用于
  已声明的 source/schema compatibility，不形成第二条生产路径。

## 5. 验证记录

- [x] `xcodebuild -list` 可解析 `MemoMark`、`MemoMarkiOS`、
  `MemoMarkShareExtension`、`MemoMarkWidgetExtension`、`MemoMarkTests` 和
  `MemoMarkDeviceQA` 方案。
- [x] `MemoMarkTests` 的 `build-for-testing` 在隔离 DerivedData 下通过。
- [x] 使用隔离 DerivedData 的无签名通用 iOS Debug `MemoMarkiOS` 构建通过；
  App、Share Extension、Widget 三个生成产物的版本均为 `2.2.4 / 101`。
- [x] 迁移后 9 项责任归属源码检查通过；`git diff --check` 通过。
- [x] 同一产品代码在版本字段变更前，实体 iPhone 17 Pro Max 的
  `MemoMarkDeviceQA` 为 `17 passed / 0 failed / 0 skipped`；其结果包已在
  `Docs/CURRENT_STATUS.md` 记录。
- [x] `2.2.4 (101)` 已在实体 iPhone 17 Pro Max 原位重新安装，并完成一次
  Apple Photos Share 发起、4 项完整 Live Photo 动态路由与 PhotoKit 保存读回。
- [x] 用户已确认 `2.2.4 (101)` 输出相册中的人工视觉与播放检查基本通过；
  该结论作为人工验收记录保存，不由 PhotoKit 资源读回自动推断。
- [ ] macOS `MemoMarkTests` 的完整可重复运行待 Xcode 测试收尾卡住问题被
  隔离；此前中断运行不得视为完整通过或完整失败。
- [ ] TX-001 的完整 D1-D5/取消/收据失败矩阵、BP-001 Instruments 测量和
  superseding production certification 仍延期到后续专项维护；当前不把本版
  标记为正式生产认证。

## 6. 授权与停止点

- [x] 用户已授权版本升至 `2.2.4 (101)`，整理本地项目并将可合并工作归入
  `main` 的当前工作树。
- [x] Git 暂存与提交：已创建提交 `ac95ab3`，提交信息为
  `refactor: close 2.2.4 architecture reorganization`。
- [x] GitHub push：已推送到 `origin/main`，远端 `main` 与本地提交一致。
- [ ] TestFlight 上传、App Store Connect 变更和 App Store 提交：均未授权。

下一步是继续使用并观察 `2.2.4 (101)`；如果后续要上传 TestFlight、修改
App Store Connect 或提交 App Store，需要另行确认最终二进制和外部发布授权。
