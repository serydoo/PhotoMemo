# MemoMark 2.1.2 (85) 同步清单

## 基线与目标

- 上一次已同步检查点：`1b3b9f7`（2026-08-13 11:04:49，Asia/Shanghai）。
- 本轮范围终点：当前工作区中已整理并准备暂存的根视图、PhotoKit/TX-001 工程修复、Device QA 工程文件与版本资料。
- 目标版本：`2.1.2 (85)`。
- 目标分支：当前 `main`；本轮仅准备版本资料和源码同步检查，不推断提交、推送、TestFlight 上传或 App Store 提交授权。
- 状态：`Source Checkpoint Ready; Release Evidence Open`。

## 正式同步范围

- iOS 根视图状态所有权整理：编辑交互、输出草稿、配置投影、生命周期和呈现状态容器；
- 相册异步加载的 Subject/Configuration 身份保护；
- PhotoKit 静态图与 Live Photo 保存的两阶段 receipt/intent 生命周期、队列恢复和幂等性边界；
- Apple ProRAW imported UTI 声明及对应输入策略回归；
- `MemoMarkDeviceQA` target、共享 scheme、QA manifest、UI harness 和非破坏性本地验证脚本；
- 对应的架构契约回归测试与工程审计记录；
- 简体中文和 U.S. English 应用内更新日志；
- `2.1.2 (85)` 版本字段、CHANGELOG、README、CURRENT_STATUS、Device QA 工程文件与本版本四份发布材料。

## 本地保留范围

- 真机 QA 运行产生的 xcresult、截图、日志、媒体和设备容器仍然不进入同步；原 `pre-root-view-optimization-2026-08-14` stash 作为可恢复备份保留，但其源码、测试、QA contract 和脚本已合并到当前工作区；
- 私人照片、人物和地点数据、真机容器、日志、截图、录屏、签名产物、DerivedData、归档包和个人 Xcode 状态；
- 未冻结 V4 研究和无法归入本轮根视图维护范围的实验材料。

## 待决定范围

- 真机手动切换/恢复矩阵、VoiceOver、Apple Photos 完整生命周期、超大媒体和 Live Photo 端到端验收；
- StoreKit、TestFlight、App Store Connect、商店截图和生产认证；
- Git 提交、推送及任何外部交付动作。

## 版本事实源分发

| 受众 | 文件 | 边界 |
| --- | --- | --- |
| 应用内用户 | `Source/PhotoMemo/PhotoMemo/zh-Hans.lproj/Localizable.strings`、`en.lproj/Localizable.strings` | 温和、完整、只描述配置中心状态连续性与本地处理边界 |
| App Store | `2026-08-14-2.1.2-app-store-whats-new.md` | 最短的用户价值，不含测试术语 |
| TestFlight | `2026-08-14-2.1.2-testflight-notes.md` | 可执行测试路径、反馈字段和隐私提醒 |
| 内部同步 | 本文件、`release-notes`、`CHANGELOG.md`、`Docs/CURRENT_STATUS.md` | 版本、范围、证据、风险和授权边界 |

## 同步前检查表

- [x] 已确定范围起点 `1b3b9f7`、marketing version `2.1.2` 和 build number `85`；`84` 仅为已废弃的准备构建。
- [x] 已将当前文件分为正式同步、本地保留和待决定三类。
- [x] App、Share Extension、Widget Extension、MemoMarkDeviceQA 和 test target 的版本字段已统一为 `2.1.2 (85)`，待构建再次确认产物版本。
- [x] 版本总表、应用内、App Store、TestFlight、CHANGELOG、README 和 CURRENT_STATUS 使用同一事实源。
- [x] 双语资源格式和键集合检查通过；应用内契约同时验证中英文键存在和用户文案边界。
- [x] `git diff --check` 通过。
- [x] 合并后聚焦测试和构建已重新执行：`PhotoMemoTests` 重点范围 `164/164`，0 failures，0 skips；`PhotoMemoiOS` 真机 SDK unsigned build 和 `MemoMarkDeviceQA` `build-for-testing` 均生成产物；macOS、iOS App 与 Device QA xctest 产物均核验为 `2.1.2 (85)`。
- [x] 自动化、真机安装启动、手动关键路径、StoreKit、App Store Connect 和生产认证保持分开记录。
- [x] 已核对未跟踪文件；QA contract、scheme、UI harness 和验证脚本属于正式同步范围，私人照片、设备材料、归档包、签名产物和本地诊断不进入范围。
- [x] 已按本轮“准备同步”范围完成显式文件暂存；提交和推送仍需单独明确授权，不能由暂存推断。
- [ ] TestFlight 上传和 App Store 提交：未授权、未执行。

## 当前证据与停止点

- 根视图、版本、PhotoKit/队列与 Device QA contract 的合并后重点范围通过 `164/164`；原构建号 `84` 的 `56/56` 仅保留为历史准备包证据。
- 当前 iOS 真机 SDK 产物为 `/tmp/PhotoMemoMergedV212B85IOSDevice/Build/Products/Debug-iphoneos/PhotoMemoiOS.app`；Device QA `.xctestrun` 为 `/tmp/MemoMarkDeviceQA85/Build/Products/MemoMarkDeviceQA_iphoneos27.0-arm64.xctestrun`。这些是本机临时验证产物，不进入 Git 同步。
- Xcode Beta 输出了既有 Apple API 弃用警告；本轮没有把警告写成失败，也没有把它们包装成生产认证证据。
- Full suite、真机手动路径、Apple Photos、StoreKit、TestFlight、App Store 和生产认证不因本地源码检查自动关闭。
- 完成检查后可达到 `Source Checkpoint Ready`，但仍不代表已提交或已发布。
