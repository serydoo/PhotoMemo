# MemoMark 配置中心根因修复本地备份清单（2.3.0 / 105）

状态：`Local Backup Candidate`（已完成范围整理；GitHub 推送未执行）
日期：2026-09-20
仓库：`/Users/rui/Desktop/PhotoMemo`
分支：`main`
基线：`989eedb`（本次整理起点）
本地备份提交：已创建（当前 HEAD，以 `git log -1` 为准）
版本：`2.3.0 / 105`

## 1. 备份目的

为多记忆对象、多预设配置上下文根因修复建立一个可追溯的本地 Git 检查点，
同时保留 GitHub 推送前的人工复核边界。本检查点不是新的 marketing version、
TestFlight 候选、App Store 提交或生产认证。

## 2. 纳入本地备份提交的范围

- Configuration Center 的编辑上下文、保存回执、配置身份、默认处理指针和会话代次。
- 新建预设、切换/删除保护、保存失败恢复、FilmMark 独立内容和配置预览路径。
- FilmMark 横向底部结果裁剪预览、文字衬底选项和阴影对比改进。
- 与上述行为直接对应的四语资源、配置/生命周期/Renderer 契约测试。
- `PROJECT_CONSTITUTION.md`、`AI_CONTEXT.md`、`Docs/CURRENT_BRIEF.md`、
  `Docs/CURRENT_STATUS.md`、产品语言和 FilmMark 架构记录。
- 已确认属于本轮工程范围的三组 FilmMark/Minimal 配置预览资源；资源哈希见下方。

## 3. 明确留在本地、不进入本次备份提交的范围

- `Docs/Outreach/` 下的对外定位、SEO 候选稿、小红书机制和其他触达材料；它们仍是
  未发布或待单独授权的外部材料。
- `/tmp` 下的构建、测试、签名和设备截图证据。
- 私人照片、设备截图、归档包、签名产物、Xcode 用户状态和临时诊断；当前工作区
  未发现这些文件被列为候选同步文件。

## 4. 资源完整性

本轮纳入的预览资源保持当前源文件字节不变：

| 资源 | SHA-256 |
| --- | --- |
| `FilmMarkPreviewBackground.imageset/film-mark-preview-background.jpeg` | `8ecbc84a6926531b89eb29c64c8b1cc54f5729f6d1b1a76f7a2f48ace2876d12` |
| `MinimalPreviewBackgroundLandscape.imageset/minimal-preview-landscape.jpg` | `4103a7b5d3888a603f657cab308ea78e6572c224262d3dc8ea90f5f784a94e14` |
| `MinimalPreviewBackgroundPortrait.imageset/minimal-preview-portrait.jpg` | `919770484362d8cee0013b5d57ea398ed2d55b8dd674e922ea9d013f4d8ac6fa` |

三个 Asset Catalog `Contents.json` 已通过 `jq empty` 校验。

## 5. 已有验证证据

- `git diff --check`：通过。
- 签名 `MemoMarkiOS` Debug 构建与深度签名校验：通过。
- 覆盖安装保留既有 App 容器；设备启动请求成功，但截图仍为锁屏。
- `ConfigurationSessionConfigurationLifecycleTests`、`ConfigurationOptionListContractTests`、
  `FilmMarkPresentationSpecificationTests`：定向通过。
- 未将锁屏截图当作 UI、VoiceOver、Photos、Share 或 Live Photo 手工验收；这些仍需
  解锁后按真实路径单独完成。
- Xcode 27 全量测试的已知 `SwiftCompile ... exit code 0 but produced no further output`
  工具链异常仍保持为未闭合，不写成全量绿灯。

## 6. GitHub 边界

本次只建立本地备份检查点，不执行 `git push`、TestFlight、App Store Connect 或其他
外部发布动作。后续推送前仍需重新复核 staged diff、当前版本事实、对外文案和未闭合
真机证据。
