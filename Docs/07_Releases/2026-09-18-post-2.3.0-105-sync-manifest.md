# MemoMark 源码检查点同步清单（post-2.3.0 / 105）

状态：`Source Checkpoint Ready`（本地材料已整理；未暂存、未提交、未推送）

## 1. 范围锚点与版本口径

- 项目：`MemoMark`；仓库目录：`/Users/rui/Desktop/PhotoMemo`
- 最近正式源码同步锚点：`56f6a17`（`Prepare MemoMark 2.3.0 build 105 release`）
- 当前 HEAD：`53f91dc`（`feat: refine configuration center and macOS parity`）
- 当前分支：`main`
- 当前远端关系：`HEAD == origin/main == 53f91dc`
- 当前 App/Extension/Widget/test target 版本字段：继续保持 `2.3.0 / 105`
- 本轮版本动作：不创建新 marketing version，不创建新 build，不修改版本字段
- 历史口径：仓库现有 2.3.0（105）材料仍以 2.2.2（95）作为正式 App Store
  历史起点；本清单只采用最近的正式源码检查点 `56f6a17` 计算增量，避免把两个
  不同层次的锚点混成一次新的正式发布。

## 2. 从锚点到当前的主要变化

### 已在 HEAD / GitHub 的变化

- macOS Configuration Center 原生布局、Inspector、共享配置会话和对应契约测试。
- iOS/macOS 配置表面与 Memory Card 预览的跨平台 parity 收口。
- 四语资源、产品语言和配置中心相关自动化保护。

### 当前工作树候选变化

- FM 独立内容/外观/布局/解析呈现模型，以及生产快照完整性和失败关闭关系。
- FM 预览、导出、Live Photo overlay、坐标桥接、CoreText fit 和底色安全边界的
  契约收口。
- FM 配置控件、位置/详情、预览表面、详情 sheet 和支持逻辑的职责拆分。
- FM 四语控件文案、原生单选控件、动态字号回退和配置界面安全区域提示。
- 对配置持久化、Share/Batch snapshot、LegacySettingsStore 兼容边界和相关测试的
  维护性加固。

## 3. 工作区分类

### 正式同步候选（用户确认后才可暂存）

- 已接受的 MemoMark 产品/工程记录及本次四份同步材料。
- FM、Configuration Center、持久化/快照、预览/布局/Renderer 路径的源码改动。
- 与上述行为直接对应的本地化资源和自动化测试。
- 用户确认纳入的 `Docs/CURRENT_STATUS.md` 状态记录。

候选文件必须在执行 `git add` 前再次按逐文件清单核对；当前没有自动暂存任何内容。

### 本地保留（不得暂存）

- 私人照片、设备截图、签名产物、归档包、DerivedData、Xcode 用户状态和临时诊断。
- `Docs/Outreach/` 对外联络/平台研究材料，除非另有明确同步授权。
- 未冻结的研究草稿、实验资产和与本次源码检查点无直接关系的个人工作材料。
- `/tmp` 下的构建、测试、签名和设备证据产物。

### 待决定（当前不得同步）

- 尚未确认是否作为长期工程记录的研究文件和审查草稿。
- 对外文案、商店草案和 TestFlight 草案：本清单只把它们作为事实投影材料，正式
  提交前仍需重新确认版本范围和用户可见承诺。
- 任何无法证明来自本次范围、或会改变既有产品/架构声明的额外改动。

## 4. 验证与证据边界

- [x] `git diff --check`：当前工作树通过。
- [x] 既有完整 `MemoMarkTests` 记录：1,865 passed / 0 failed / 1 skipped。
- [x] 既有 macOS Debug、通用 iOS、Share Extension Debug 构建记录通过。
- [x] 既有治理校验通过。
- [x] 签名 iPhone 17 Pro Max 包曾覆盖安装并启动；未将此扩大为人工验收。
- [ ] 真机视觉、Dynamic Type、VoiceOver、保存重开、Photos、Share、Live Photo。
- [ ] TX-001 外部提交中断/精确读回/重复输出矩阵。
- [ ] BP-001 高分辨率内存峰值、并发和导出延迟实测。
- [ ] 新鲜且可归因的当前 workspace COS 独立复核。
- [ ] TestFlight、App Store Connect、Xcode Cloud 或生产发布证据。

已知非阻断项仍需保留在记录中：一个 ImageIO fixture skipped，以及
`FixtureExportReadbackTests` 报告的两条 QoS priority-inversion warning；不得把
警告或跳过项改写成“全部通过”。

## 5. 同步停止点与授权

本轮已完成：事实整理、范围解释、四份材料草案、候选分类和本地检查点说明。

本轮尚未执行：`git add`、commit、push、TestFlight 上传、App Store Connect 操作、
Xcode Cloud 触发、版本字段修改或新的外部 COS 调用。

原因：仓库当前仍是混合脏工作树，工作树候选与本地保留/待决定文件尚未通过用户的
逐层授权；`scripts/auto_sync_to_github.sh` 会无差别 `add -A`、提交并推送，不作为
本轮执行路径。后续若用户明确授权 Git 同步，应先按逐文件清单暂存并复核 staged diff，
再单独取得提交和推送授权；任何 TestFlight/App Store 动作都必须另外授权。
