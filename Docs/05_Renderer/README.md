# 05 Renderer

Renderer contracts, renderer QA, snapshot workflows, and renderer migration notes belong here.

In V2, renderer docs must treat renderers as stateless drawing surfaces. Layout belongs to the Layout Engine.
