# DesignSystem

This folder is reserved for durable MemoMark V2 design-system assets.

It should eventually contain:

- color tokens
- typography tokens
- spacing tokens
- grid tokens
- layout tokens
- optical layout rules

Renderer code must not become the source of these values.
