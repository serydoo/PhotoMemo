# Renderer

This folder is reserved for the future V2 renderer boundary.

Renderer responsibility:

- draw resolved layout instructions
- preserve output fidelity
- avoid layout decisions

Renderer must not calculate padding, spacing, font sizes, slot widths, or adaptive layout rules.
