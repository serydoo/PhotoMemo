#if !MEMOMARK_SHARE_EXTENSION
import Foundation

/// Cross-layer configuration transport types own responsibility-based names
/// at their declaration sites. Persisted keys and raw values remain the only
/// compatibility boundary for these transport representations.

#endif
