# App

This folder is reserved for the future V2 app-facing entry structure.

Current app source lives under `Source/MemoMark/` after the completed internal
project naming migration. The repository root `App/` folder remains reserved
for a future V4 app-facing entry structure.

Do not move source files here without a reviewed migration plan.
